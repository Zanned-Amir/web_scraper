<p style="color: green"><%= notice %></p>

 <div class="flex justify-between">
  
    <h1 class="text-2xl font-bold trancate ">
      <%= link_to  "<", root_path ,class:" p-2 rounded hover:bg-gray-100"%>
      <%= @page.name %> 
    </h1>
    <div class=" flex gap-1">
       <%= button_to "Run" , page_check_index_path(@page), class:"bg-green-100 border-green-200 rounded shadow px-2 py-1 hover:bg-green-300"%>
       <%= link_to "Edit", edit_page_path(@page) , class:"bg-yellow-100 border-yellow-200 rounded shadow px-2 py-1 hover:bg-yellow-300"%>
    </div>

  </div>
   <%= link_to @page.url, @page.url , class: "text-gray-700 mt-2", target: :_blank%>
<div class="mt-4">
  <h4 class=" text-gray-700 font-medium">Recent results</h4>

<%= render  @page.results.order(created_at: :desc) %> 

</div>

